<Window x:Class="MVVM_Learning.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" mc:Ignorable="d"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:MVVM_Learning.ViewModels"
        xmlns:local="clr-namespace:MVVM_Learning"
        Title="{Binding Path=Title}" 
        Width="800" Height="450" >
    <Window.DataContext>
            <vm:MainWindowViewModel/>
    </Window.DataContext>
        
    <Grid>
        <TextBox Margin="66,44,0,0" 
                 Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" TextWrapping="Wrap" 
                 HorizontalAlignment="Left" VerticalAlignment="Top"
                 Width="120"/>

    </Grid>
</Window>
